# 按下 Esc 之后，我的 Agent 就"失忆"了：一次关于「取消」的排查

> 日期：2026-09-15
> 项目：agent lite —— 用 Python 复刻自己的 pi agent 类物
> 阶段：Day 5 —— 中断恢复：把"用户取消"当成一等公民
> 方向：自底向上分层（ai → agent_core → coding_agent），底层不能调用上层

## 一、背景

前一天我给 Agent 加上了按 Esc 中止的能力。想法很简单：它跑偏了，或者我改主意了，我按一下 Esc，它就该停下手里的活，把控制权还给我。

实现看起来也很"显然"：在循环的几个检查点看一眼"用户是不是要中止"，是就跳出循环，回一句「（已中止）」。

我当时的判断是：**取消是一个收尾动作**——提前结束而已，随便收一下就行了。

这个判断后来被证明是错的，而且错得挺贵。

## 二、问题是怎么被发现的

第一次按 Esc，一切正常：它停下来了，屏幕上出现「（已中止）」。我挺满意的。

然后我接着把下一句话发了出去。

请求被接口直接拒了。大意是：这条 assistant 消息声明了要调用工具，但后面找不到对应的工具结果。

翻译成人话就是：**前面说了"我要做这三件事"，后面却只有一件有回音。这种半截记录，接口不接受。**

## 三、翻存档：一条断掉的记录

我打开会话存档，看到的画面大概是这样：

```
assistant：我要调用 3 个工具
   ├─ read   main.py     → 有结果 ✅
   ├─ edit   main.py     → 没有下文 ❌
   └─ bash   pytest      → 没有下文 ❌
```

问题很清楚了：我是在"第 2 个工具还没执行"的时候按的 Esc。而代码在那里的写法是「如果用户要中止，就 break」——直接跳出循环。

于是那两个工具调用，就永远等不到自己的结果。

## 四、真正的问题：对话历史不是聊天记录

动手修之前，我先改了自己的一个认知。

我一直把会话历史当成"聊天记录"——给人看的，看得懂就行。其实它不是。它是一份要跟接口一条条对齐的**协议记录**：模型每说一句"我要调用工具"，就必须有一条对应的"结果"跟上来，一条都不能少。

所以"少了两条"不是"记录不太完整"这么轻描淡写，而是**整份历史变成非法，这个会话直接废掉**。

更让我在意的是另一点：这根本不是什么偶发路径。用户按 Esc，是这个工具里每天会发生几十次的操作。**把一个高频操作当成异常来处理，是我的设计失误。**

## 五、难点不在"补一条"，在"取消可以在任何时刻发生"

我的第一反应是：那取消的时候，给没跑的工具补一句"已取消"不就行了？

补是补了，但往下想才发现真正的麻烦：**取消可能发生在任何一个时刻，每种时刻需要补的东西都不一样。**

| 按 Esc 的时刻 | 存档里已经有什么 | 该怎么收尾 |
| --- | --- | --- |
| 模型还没说要调用工具 | 什么都没有 | 直接结束 |
| 刚说要调用，一个都还没跑 | 声明 3 个，结果 0 个 | 补 3 条"没有执行" |
| 跑到一半 | 声明 3 个，结果 1 个 | 补 2 条"没有执行" |
| 全都跑完了，模型还在总结 | 声明 3 个，结果 3 个 | 谁的账都不欠，但**绝对不能重跑** |

最后一行特别容易被忽略：已经拿到结果的工具，绝不能再跑一遍。不然我按一次 Esc，它可能把已经改好的文件再改一次。

## 六、补进去的那句话，必须是真话

把没执行的工具一律补成"失败"，最省事——但那是撒谎。模型看到"失败"，会去分析失败原因，甚至去修一个根本没坏的东西。

补成"成功"更糟：它会以为活已经干完了。

所以补进去的这句话必须说实话：

> Tool execution cancelled by the user; this call did not run.
> （用户中止了执行，这个调用没有运行。）

它既不是成功也不是失败，是"我叫停了"。模型拿到这句真话，才知道下一步该干什么。

## 七、还有一个更隐蔽的时刻

补完这些，我以为可以收工了。结果又想到一种情况：

我按 Esc 的时候，模型可能正在"往外吐工具参数"——参数才吐了一半。

这时候流里那些还没说完整的工具调用，也必须一起丢掉。否则历史里会出现一个**残缺的工具调用**：名字有了，参数只有半截。那比"没有结果"更麻烦。

于是最终定下来的规矩只有一句话：

> 无论在哪一刻中止：历史里每一条"我要调用工具"，都必须有一条对应的结果。

钉住这个行为的测试，名字就叫 `test_cancel_between_tools_keeps_history_valid_for_continue`——**取消之后，这份历史依然能继续对话。**

## 八、这次踩坑真正训练了什么

1. **"用户取消"不是异常，是最高频的正常操作之一。** 它必须产出一个合法的状态，而不是"能停下来就算成功"。
2. **Agent 的对话历史不是日志，是一份必须随时合法的记录。** 少一条，整个会话就废——这是我以前从没意识到的脆弱点。
3. **边界情况要按时间轴穷举，而不是写一个 break。** "还没开始 / 刚开始 / 中途 / 刚好完成"这四种时刻，处理方式完全不同。
4. **补记录必须是真话。** 用"失败"或"成功"糊弄模型，只会换来它一次错误的判断。
5. 我对"中止"的第一反应是"提前结束"，但对 Agent 来说，中止是**一次状态迁移**——得把账结清才能走。

## 九、下一步

- **打开会话时先自检**：检查工具调用和结果是否一一配对，发现不合法就明确告诉我"这个会话的哪一段断了"，而不是等我发下一句话、被接口拒了才知道。
- 把中断恢复继续往细做。
