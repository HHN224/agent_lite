# WSL 明明运行在虚拟机里，为什么还护不住 C 盘：一次从错误安全假设到 Bubblewrap 落地的改造

> 日期：2026-09-09
> 项目：agent lite —— 用 Python 复刻自己的 pi agent 类物
> 阶段：命令执行安全 —— 从裸 WSL 到 WSL + Bubblewrap
> 方向：在 Host 和 Docker 之间补一个轻量、可验证的沙箱层

## 背景

我的项目里有一个 Bash 工具，Agent 可以通过它执行命令。为了适应不同的开发环境，我一开始做了三种执行后端：

- Host：直接在 Windows 宿主机上执行，最方便，也最危险；
- WSL：把命令交给 Linux 环境执行，我原本把它当成中间档；
- Docker：隔离最完整，但启动、镜像和环境维护都更重。

当时我的直觉是：WSL2 本身运行在轻量虚拟机里，至少应该比直接在 Host 上执行安全不少。于是自动选择后端时，我把它放在 Docker 和 Host 之间。

这个判断听起来很自然，但后来我意识到，它混淆了两个完全不同的问题：

> “命令是不是运行在虚拟机里”，和“命令能不能访问宿主机的重要文件”，并不是一回事。

这次改造最有价值的地方，不是最后加上了 Bubblewrap，而是我及时发现原来的安全假设站不住脚，并重新定义了这个执行层真正需要解决的问题。

## 问题是怎么被发现的

最初的 WSL 执行逻辑很简单：把 Windows 工作区路径转换成 WSL 路径，再通过 `wsl.exe` 执行命令。例如：

```text
D:\project space\agent lite
        ↓
/mnt/d/project space/agent lite
```

看到这条路径映射时，我突然意识到一个问题：项目目录之所以能从 WSL 访问，是因为 Windows 磁盘已经被挂载到了 `/mnt`。既然 D 盘在 `/mnt/d`，那么 C 盘通常也在 `/mnt/c`。

也就是说，Agent 虽然没有直接在 Windows shell 中运行，但它仍然可能读取甚至修改：

- 用户目录和桌面文件；
- SSH 密钥、配置文件和浏览器数据；
- 其他项目源码；
- C 盘和 D 盘上任何当前 Windows 用户有权限访问的内容。

这时，裸 WSL 的真实安全位置就很清楚了：它提供了 Linux 运行环境和一定程度的系统边界，却没有提供“只能访问当前工作区”的文件系统边界。

换句话说，**裸 WSL 更像一个方便的 Linux 执行环境，而不是一个默认安全的命令沙箱。**

这是我在这次工作中最重要的一次判断修正。安全问题经常不是因为代码完全没有防护，而是因为某个“看起来应该安全”的组件被赋予了它实际并不具备的保证。

## 先确定目标，而不是先追求最强隔离

发现问题后，我没有立刻把所有命令都强制塞进 Docker。因为项目仍处于开发阶段，我真正需要的不是一套面向恶意软件的完整防御体系，而是一个明确的中间档：

```text
Host < 裸 WSL < WSL + Bubblewrap < Docker
```

这个中间档需要做到：

1. 比直接在 Host 或裸 WSL 中执行安全；
2. Agent 默认只能写当前项目目录；
3. 看不到 Windows 的 C 盘和 D 盘；
4. 默认不能联网；
5. 保持轻量，不需要为每次执行启动容器；
6. 不追求与 Docker 完全相同的安全强度。

这一点对后面的技术选择很重要。如果目标一开始就写成“做到绝对安全”，方案很容易膨胀成另一套容器平台；如果只写成“比 Host 安全一点”，又很难验证到底安全在哪里。

我最后把目标收敛为一句话：

> 保留 WSL 的低使用成本，但把每条 Bash 命令放进一个只能看到当前工作区的临时 Linux 命名空间。

## 为什么最后选择 WSL + Bubblewrap

我调查过几类替代方案。

单纯关闭 WSL 的 Windows 磁盘自动挂载，确实能减少暴露面，但项目本身就在 D 盘，完全关闭后会破坏当前开发流程；而且这仍然是在调整整个 WSL 发行版，不是为单条命令建立明确边界。

Windows Sandbox 和 Hyper-V 虚拟机的隔离更强，但它们偏向完整系统，启动和环境同步成本更高。Docker 已经覆盖了这个强隔离档位，再做一套类似方案没有太大意义。

Bubblewrap 则刚好符合中间档需求。它不能直接作为 Windows 原生程序运行，因为它依赖 Linux 的 namespace 等内核能力；但 WSL2 本身提供了 Linux 内核，所以可以在 WSL 内部使用 Bubblewrap。

最终的执行链路是：

```text
Windows 上的 Agent
        ↓ wsl.exe
WSL2 Linux 环境
        ↓ bwrap
临时命名空间
  ├─ /workspace：当前项目，可读写
  ├─ /：只读
  ├─ /mnt：空目录，看不到 C/D 盘
  ├─ /tmp、/home、/root：临时文件系统
  └─ 网络：隔离
```

这不是“让 WSL 本身再也看不到 Windows 磁盘”。外层 WSL 为了正常开发仍然可以挂载 D 盘。真正的边界在 Bubblewrap 创建的子进程里：它使用独立的挂载命名空间，把 `/mnt` 替换成空的临时文件系统，只把当前项目重新绑定到 `/workspace`。

这个区别很关键。我们没有为了沙箱破坏整个 WSL 的可用性，而是把限制精确施加在 Agent 启动的命令上。

## 利用已有接口，把改动限制在正确的位置

项目之前已经把命令执行抽象成统一的 `CommandRunner` 接口，Host、WSL 和 Docker 都只是不同实现。这个设计在这次改造中发挥了作用：上层 Agent 和工具调用逻辑完全不需要理解 Bubblewrap。

我直接升级了现有 `WslRunner`：

- 保持原来的 `--sandbox wsl` 用法；
- 不新增另一套 Runner；
- 不新增发行版选择和复杂策略配置；
- 将工作区映射到沙箱内固定的 `/workspace`；
- 用一组固定的 Bubblewrap 参数提供只读根目录、工作区读写、磁盘隐藏和网络隔离；
- 检测 WSL 时顺便检查 `bwrap` 是否存在。

这样做的好处是，安全实现被放在了原本就负责命令边界的位置，调用方看到的接口没有变化。整个功能改动最终集中在 Runner、测试和 README 三处。

## 一次及时的“减法”：拒绝开发期过度设计

最开始的方案其实比最终实现复杂得多：新的适配层、发行版参数、能力探测、更多配置对象、资源限制和大量失败兜底都被考虑了进去。

从生产系统角度看，这些内容未必错误，但它们不适合当前阶段。项目还在快速开发，如果一次加入过多防御性分支，不仅会增加代码量，也会模糊这次改造最核心的目的。

因此我重新砍掉了不必要的内容，只保留能够形成实际安全边界的最小集合。当前版本没有尝试用 cgroup 模拟 Docker 的内存和进程数限制，也没有为所有 WSL 发行版差异建立兼容层。

这次范围调整也让我更明确了一件事：工程能力不只是想到更多可能性，也包括知道哪些问题现在不需要解决。

## 单元测试通过了，真实环境却暴露了问题

代码完成后，针对沙箱的 14 个测试全部通过，完整测试套件也通过了 193 个测试。但第一次在真实 WSL 中运行 Bubblewrap 时，仍然失败了：

```text
bwrap: Can't mkdir /workspace: Read-only file system
```

原因是参数先把根目录绑定成了只读，而 `/workspace` 这个挂载点在 WSL 中并不存在。Bubblewrap 想创建它时，自然无法在只读根目录上写入。

最后的修复很小：在 WSL 初始化步骤中预先创建 `/workspace`，并把这一步写进 README。

但这个小故障很有代表性。Mock 测试可以证明参数拼接正确，却不能证明 Linux 挂载顺序和真实文件系统状态一定可用。如果我只看到测试全绿就结束工作，这个功能实际上交付后仍然无法运行。

所以我又补了一次真实冒烟验证，确认沙箱内：

- 当前目录确实是 `/workspace`；
- 能够在工作区创建和修改文件；
- `/mnt/c` 不可见；
- `/etc` 不可写；
- 没有可用网络路由；
- 命令可以正常结束并返回结果。

这一步把“代码看起来建立了沙箱”变成了“关键安全属性已经在真实环境中得到验证”。

## 当前结果和边界

改造完成后，WSL 后端不再等同于“进入 WSL 后直接执行”。每条 Bash 命令都会再经过 Bubblewrap，并受到以下限制：

- 只有当前项目目录可写；
- Linux 根目录只读；
- Windows 磁盘挂载在沙箱内不可见；
- 临时目录和用户目录不会复用外层 WSL 的真实内容；
- 默认使用独立网络命名空间；
- 子进程会随着父进程结束。

但我也不想把它包装成 Docker 的完全替代品。当前边界仍然很明确：

- Bubblewrap 只保护 Bash 工具启动的 Linux 命令，不会隔离 Windows 上运行的 Agent 主进程或其他原生工具；
- 它与 WSL2 共享 Linux 内核，不用于抵御内核逃逸或高级恶意代码；
- 当前没有 Docker 后端已有的内存上限和进程数上限；
- 外层 WSL 仍然能看到已挂载的 Windows 磁盘，只是 Bubblewrap 内的命令看不到；
- 对真正不可信、风险更高的任务，Docker 仍然是更合适的后端。

因此，这次实现的价值不是取代 Docker，而是让原本过于危险的“方便模式”拥有一个成本较低、边界清楚的保护层。

## 这次改造真正训练了什么

回头看，技术实现本身并不庞大，真正有价值的是完整的问题解决过程：

1. 没有接受“WSL 在虚拟机里，所以它是沙箱”这个直觉结论；
2. 从实际路径映射追踪数据可见范围，发现 C/D 盘暴露；
3. 先定义项目当前阶段需要的安全等级，再选择工具；
4. 找到 WSL 与 Bubblewrap 的组合，用两层不同能力形成边界；
5. 利用已有抽象完成局部替换，避免改动上层架构；
6. 发现方案过度设计后主动缩小范围；
7. 没有停在单元测试，而是通过真实环境验证发现并解决挂载点问题；
8. 明确记录剩余风险，没有把“更安全”描述成“绝对安全”。

对我来说，这比单纯写出一个 Bubblewrap 命令更重要。它说明我开始能够从系统实际行为出发审视安全假设，也能在安全性、开发成本和项目阶段之间做出可解释的取舍。

## 下一步

目前这套 WSL + Bubblewrap 后端已经能够承担日常开发中的轻量沙箱角色。接下来我更希望先观察真实使用情况，而不是马上继续增加配置。

如果后续出现明确需求，可以再逐步补充：

- 更细粒度的网络开关；
- CPU、内存和进程数限制；
- 对不同 WSL 发行版的安装和兼容性检查；
- 更系统的逃逸测试和权限回归测试。

但在现阶段，这次改造已经达成了最重要的目标：我不再依赖一个错误的安全假设，而是拥有了一条轻量、可运行、可验证，并且知道边界在哪里的执行路径。
