# 沙箱里没有网络，可模型不知道：一次把「处境」写进工具的改造

> 日期：2026-09-15
> 项目：agent lite —— 用 Python 复刻自己的 pi agent 类物
> 阶段：Day 6 —— 让模型知道自己跑在什么环境里
> 方向：把环境约束写进工具的说明书，而不是等它踩坑

## 一、背景

前一阵我花了不少力气，把 bash 工具关进了 WSL + Bubblewrap 沙箱：只能写当前项目目录、看不见宿主机的 C 盘 D 盘、默认没有网络、临时目录每次命令结束就丢。

从安全角度看，我很满意。

从"好不好用"的角度看，问题很快就来了。

## 二、现象：Agent 在沙箱里像个刚进城的

我让它跑一个小脚本，它干的事大概是：

先 `ls` 一圈 → 去 `/mnt` 里翻宿主目录 → 找 node → 找 npm → 找 python → 试 `pip install` → 试 `npm install` → 换个路径再找浏览器 → 再换一种命令写法继续试。

结局和 Day 2 那个编码 bug 一模一样：**轮数被烧光，活没干完，最后吐一句没头没尾的结束语。**

而它全程都没有说过一句"这个环境里装不了东西"。它只是一直在**换一个命令再试试**。

## 三、真正让人意外的地方

我一开始的想法很自然：那就把这些限制写进系统提示词，告诉它"你在沙箱里，没有网络"。

但动手之前我翻了一遍代码，发现了一件让我有点脸红的事：

**这些信息，程序里早就有了。**

每个命令执行后端都有一个 `describe()`，用一句人话描述自己是什么环境。比如 WSL 后端那句就写得很清楚：无网络、系统只读、只有工作目录可写。

而那句话的唯一用途，是启动的时候打印在屏幕上，**给我看的**：

```
>>> bash 沙箱: wsl —— WSL2 + Bubblewrap（免 Docker daemon）：无网络、系统只读，仅工作目录映射为可写的 /workspace
```

也就是说：**程序知道，我知道，模型不知道。**

我花了很大精力把沙箱做得严丝合缝，但模型对这个沙箱的全部认知是零。它只能靠一条一条命令去"摸"这个世界的边界，而每摸一次，烧掉一轮。

## 四、还有一个我自己给自己挖的坑

这里有个我前一天刚埋下的矛盾。

为了让 Agent 一启动就能看到界面（而不是先卡在探测 Docker / WSL 上几十秒），我把沙箱探测推迟到了第一次真正执行命令的时候。

于是问题来了：

如果这句环境描述要"探测完了才能生成"，那模型第一次调用工具的时候就拿不到它——而**第一次恰恰是最需要它的时候**。第一次就撞墙，后面全是瞎试。

所以结论是：这条描述必须是**静态的**——不启动沙箱、不做任何探测，就能立刻给出来。

对应的测试叫 `test_wsl_environment_is_available_in_tool_schema_without_starting_sandbox`：不启动沙箱，也要能拿到环境描述。

## 五、修法：挂到模型一定会看的地方

每个工具都有一份给模型看的"说明书"（工具描述），模型每一次请求都会看到它。所以正确的挂点是这里。

我把环境描述拼进了 bash 工具的说明书里，而且写的不是"介绍环境"，是**规矩**：

```
Execution environment: WSL2 + Bubblewrap（免 Docker daemon）：无网络、系统只读，
仅工作目录映射为可写的 /workspace（也是命令的当前目录）。
宿主 /mnt、/home、/root 不可见；/tmp 等临时目录在每次命令结束后丢弃。
不能访问宿主浏览器或靠下载补齐依赖；需要跨命令保留的文件放在工作目录。

Use workspace-relative paths with read/write/edit. Check required runtimes once;
Node, npm, pip and browsers are not guaranteed to exist. If unavailable, use available
tools or report the specific missing capability. Do not repeatedly search host directories
or retry downloads when the sandbox has no network.
```

最后那几句是最关键的：**只检查一次运行时；缺什么就直接说缺什么；没有网络就别反复尝试下载。**

前者只是"介绍"，后者才是"行为约定"。

## 六、验证

我加了两条测试钉住这件事：

- 不启动沙箱，也能拿到这段环境描述；
- 描述里确实带着 WSL 沙箱的关键约束。

至于实际效果，我没法用一句话证明它"再也不会"去瞎试——模型的行为永远有随机性。但至少从这一刻起，**这些规矩在它第一次调用工具之前就已经在它手上了**，它去试探这个世界的理由少了一大半。

## 七、同一件事带出来的两个兄弟问题

顺着"程序得替模型管好它自己的身体"这条线，同一个提交里还修了两个同源的小问题：

- **子进程会抢终端。** 之前跑命令时，子进程和界面共享同一个终端，甚至可能读到我还没发出去的草稿。现在跑命令时把标准输入关掉，也不让它共享控制台。
- **WSL 会在正常输出前面塞一段系统警告。** 那段警告是另一种编码，和正常输出混在一起解码，两边都会变乱码。现在把这段单独解出来，再拼回正文。

## 八、这次改造真正训练了什么

1. **模型不是不聪明，是不知道自己的处境。** 它在沙箱里反复试错，不是能力问题，是信息问题。
2. **"写在程序里"不等于"模型知道"。** 我明明写了 `describe()`，却只打印给人看——这是一种非常典型的"我以为我已经告诉它了"。
3. **环境约束要写进工具的说明书，而且要写成可行动的规矩。** "这里没有网络"是事实；"没有网络，就别反复尝试下载，缺什么直接说"才是能改变行为的话。
4. **提示词不是唯一的入口，而且往往离问题最远。** 工具描述离"这件事"最近，模型每次必看，这才是对的挂点。
5. **启动速度和信息完整性会打架**（我不想等探测，却又必须立刻给出描述），这类矛盾只能靠"把描述做成静态的"来解开。

## 九、下一步

- 把"环境能力清单"做成可验证的：到底有没有 python、有没有 node，探测一次，把结果写进描述里。
- 切换沙箱后端（docker / wsl / host）时，这份描述要跟着变——让模型每次都知道自己换了一副身体。
